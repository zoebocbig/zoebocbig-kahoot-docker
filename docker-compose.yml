version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "5000:5000"   # API + frontend React si servi par Flask
    volumes:
      - ./backend:/app   # Pour dev live sur backend
    networks:
      - app-network


networks:
  app-network:
    driver: bridge
