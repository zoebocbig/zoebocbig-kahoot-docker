#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_path_1 = __importDefault(require("node:path"));
const kleur_1 = __importDefault(require("kleur"));
const tildify_1 = __importDefault(require("tildify"));
const yargs_1 = __importDefault(require("yargs"));
const helpers_1 = require("yargs/helpers");
const _1 = require("./");
const parser = (0, yargs_1.default)((0, helpers_1.hideBin)(process.argv))
    .command('$0 [name]', 'The default command', (yargs) => yargs.positional('name', {
    describe: 'Module name or directory path',
    type: 'string',
    default: '.',
}))
    .option('system', {
    alias: 's',
    describe: 'Module system',
    choices: ['esm', 'cjs'],
    default: 'esm',
})
    .option('identifier', {
    alias: 'i',
    describe: 'Module system identifier',
    choices: ['file-ext', 'package-type', 'syntax'],
    default: 'file-ext',
})
    .alias('help', 'h')
    .alias('version', 'v')
    .usage('$0 [name] [options]')
    .strict();
(() => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const { name, system, identifier } = yield parser.parse();
        console.log(kleur_1.default.green('Creating module...'));
        const files = yield (0, _1.createModule)(node_path_1.default.resolve(process.cwd(), name), system, identifier);
        for (const file of files) {
            console.log(kleur_1.default.green(`+ ${(0, tildify_1.default)(file)}`));
        }
        console.log(kleur_1.default.green('Module created!'));
    }
    catch (error) {
        console.error(kleur_1.default.red('An error occurred!'));
        throw error;
    }
}))();
